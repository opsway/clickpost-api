{
  "name": "opsway/clickpost-api",
  "description": "PHP SDK for Clickpost Indian Provider",
  "license": "MIT",
  "keywords": [
    "Clickpost",
    "API",
    "SDK",
    "library"
  ],
  "support": {
    "issues": "https://github.com/opsway/clickpost-api/issues"
  },
  "prefer-stable": true,
  "require": {
    "php": "^7.2",
    "guzzlehttp/guzzle": "^6.3 || ^7.2",
    "ext-json": "*"
  },
  "require-dev": {
    "phpunit/phpunit": "^8.0.2",
    "squizlabs/php_codesniffer": "^3.4.0"
  },
  "autoload": {
    "psr-4": {
      "OpsWay\\Clickpost\\": "src/"
    }
  },
  "scripts": {
    "test": "phpunit --no-coverage",
    "test-cover": "phpunit",
    "cs-check": "phpcs -n --standard=PSR2 --extensions=php src/",
    "cs-fix": "phpcbf --standard=PSR2 --extensions=php src/"
  }
}
